example-plugin:
  type: object
  properties:
    cli:
      type: object
      properties:
        name:
          type: string
          parameter: name
          export_env: NAME
          required: true
    options:
      type: object
      properties:
        favorite_color:
          type: string
          export_env: FAV_COLOR
